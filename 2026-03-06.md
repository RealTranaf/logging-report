<h1 align="center">Thực hiện logging cho một trang web</h1>

Trang web cần log: ReactJS frontend, Spring Boot backend, CSDL MySQL

FE: https://github.com/RealTranaf/logging-fe

BE: https://github.com/RealTranaf/logging-be

Spring Boot có sẵn chức năng logging thông qua thư viện SLF4J. Thường sẽ log các action ở tầng service và tầng controller để ghi lại hoạt động của API và các chức năng trong BE.

**Logging level**

Các thư viện logging sẽ có 7 mức độ log khác nhau:

1. Fatal: lỗi cực kì nghiêm trọng, hệ thống buộc phải dừng. Không phải thư viện logging nào cũng có mức này.

2. Error: lỗi nghiêm trọng, thường là một bộ phận hay chức năng xảy ra lỗi hoặc exception xảy ra.

3. Warn: mức độ cảnh báo, có vấn đề tiềm ẩn nhưng hệ thống vẫn chạy. Có thể dùng để log những hành động nhạy cảm như xóa dữ liệu.

4. Info: thông tin hoạt động bình thường, dùng để log các sự kiện và hoạt động thông thường của hệ thống.

5. Debug: log chi tiết, sâu hơn để kiểm tra logic.

6. Trace: Mức chi tiết sâu nhất

Thứ tự: TRACE < DEBUG < INFO < WARN < ERROR < FATAL

Ở tầng service, thường sẽ log kiểu như sau:
```
public List<Game> filterGames(Platform platform, PubStatus status) {
    log.info("Filtering games with platform={} status={}", platform, status);

    Specification<Game> spec = Specification
            .where(GameSpecification.hasPlatform(platform))
            .and(GameSpecification.hasStatus(status));

    List<Game> games = gameRepository.findAll(spec);
    log.info("Found {} games", games.size());

    return games;
}
```

Ghi log trước và sau khi thực hiện chức năng logic của function. Thực hiện logging ở đây để kiểm tra xem dữ liệu được thay đổi thế nào và logic của hệ thống có được thực hiện thành công hay không.

Để log xem API nào được gọi và thời gian phản hồi của nó, cần thực hiện log ở tầng controller. Trong backend build từ Spring Boot này, có thể thêm một interceptor (class can thiệp) để thực hiện logging:

```
@Slf4j
@Component
public class RequestLoggingInterceptor implements HandlerInterceptor {
    private static final String START_TIME = "startTime";

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        long startTime = System.currentTimeMillis();
        request.setAttribute(START_TIME, startTime);
        log.info("Incoming request: {} {}", request.getMethod(), request.getRequestURI());
        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest request,
                                HttpServletResponse response,
                                Object handler,
                                Exception e) {
        long startTime = (Long) request.getAttribute(START_TIME);
        long duration = System.currentTimeMillis() - startTime;
        log.info("Completed request: {} {} -> {} ({} ms)",
                request.getMethod(),
                request.getRequestURI(),
                response.getStatus(),
                duration);
        if (e != null) {
            log.error("Request error: {}", e.getMessage(), e);
        }
    }
}
```

```
@Configuration
@RequiredArgsConstructor
public class WebConfig implements WebMvcConfigurer {
    private final RequestLoggingInterceptor requestLoggingInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(requestLoggingInterceptor);
    }
}
```

Tương tự như khi log ở service, ở controller sẽ log trước và sau khi thực hiện API. Sẽ log cụ thể API nào được gọi và response time của nó.

Một đoạn log ví du:
```
2026-03-09T10:36:58.371+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-5] c.v.l.config.RequestLoggingInterceptor   : Incoming request: GET /games/13
2026-03-09T10:36:58.371+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-5] com.vtc.logging.service.GameService      : Fetching game with id=13
2026-03-09T10:36:58.379+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-5] c.v.l.config.RequestLoggingInterceptor   : Completed request: GET /games/13 -> 200 (8 ms)
2026-03-09T10:37:04.857+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-6] c.v.l.config.RequestLoggingInterceptor   : Incoming request: OPTIONS /games/13
2026-03-09T10:37:04.857+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-6] c.v.l.config.RequestLoggingInterceptor   : Completed request: OPTIONS /games/13 -> 200 (0 ms)
2026-03-09T10:37:04.866+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-7] c.v.l.config.RequestLoggingInterceptor   : Incoming request: DELETE /games/13
2026-03-09T10:37:04.866+07:00  WARN 1040 --- [Logging] [http-nio-8080-exec-7] com.vtc.logging.service.GameService      : Deleting game with id=13
2026-03-09T10:37:04.877+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-7] com.vtc.logging.service.GameService      : Game deleted successfully id=13
2026-03-09T10:37:04.877+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-7] c.v.l.config.RequestLoggingInterceptor   : Completed request: DELETE /games/13 -> 200 (11 ms)
2026-03-09T10:37:04.886+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-8] c.v.l.config.RequestLoggingInterceptor   : Incoming request: GET /games
2026-03-09T10:37:04.886+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-8] com.vtc.logging.service.GameService      : Filtering games with platform=null status=null
2026-03-09T10:37:04.896+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-8] com.vtc.logging.service.GameService      : Found 8 games
2026-03-09T10:37:04.896+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-8] c.v.l.config.RequestLoggingInterceptor   : Completed request: GET /games -> 200 (10 ms)
```

Action ở frontend: Ấn vào một item trên trang web, thực hiện yêu cầu xóa item đó, refresh trang web để lấy danh sách item mới sau khi xóa.

Có thể thấy từ log, frontend đã call API để lấy dữ liệu của item đó (GET /games/13) do là để truy cập chức năng xóa, cần ấn vào một item để mở modal chứa thông tin của item đó cùng với các chức năng sửa và xóa. Frontend gọi API delete (DELETE /games/13), thực hiện thành công và refresh trang, lấy dữ liệu từ API get (GET /games).

Có thể thấy log ghi lại các API được gọi, các action ở phần logic, trạng thái thực hiện thành công và các mức độ logging khác nhau.

Cụ thể lấy ví dụ ở phần này:

```
2026-03-09T10:37:04.886+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-8] c.v.l.config.RequestLoggingInterceptor   : Incoming request: GET /games
2026-03-09T10:37:04.886+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-8] com.vtc.logging.service.GameService      : Filtering games with platform=null status=null
2026-03-09T10:37:04.896+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-8] com.vtc.logging.service.GameService      : Found 8 games
2026-03-09T10:37:04.896+07:00  INFO 1040 --- [Logging] [http-nio-8080-exec-8] c.v.l.config.RequestLoggingInterceptor   : Completed request: GET /games -> 200 (10 ms)
```

Log cụ thể thời gian, mức độ log, file nào thực hiện log. Những đoạn log sinh ra từ 1 lần gọi API sẽ được nhóm với nhau để dễ dàng phân tích hành động của hệ thống (ví dụ như đây, thực hiện gọi GET /games, các đoạn log cho hoạt động này đều đánh dấu bằng http-nio-8080-exec-8)

Note: có thể customize log của SLF4J. VD:

```
logging.pattern.console=%d{HH:mm:ss} | %-5level | %logger{36} | %msg%n
```

sẽ sinh ra log kiểu:

```
10:45:02 | INFO  | GameController | GET /games called
10:45:02 | INFO  | GameService    | Found 5 games
10:45:03 | WARN  | GameService    | Deleting game id=7
```

hay 

```
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
```

sẽ thành

```
2026-03-09 10:48:10 [http-nio-8080-exec-3] INFO  GameController - GET /games called
2026-03-09 10:48:10 [http-nio-8080-exec-3] INFO  GameService - Found 4 games
```

(Đối console thành file để chỉnh sửa log ghi ra file)

**Config file log kĩ hơn**

Để thực hiện config file log của Slf4j (Logback) kĩ càng hơn, cần sử dụng file config là logback-spring.xml. Tại đây có thể config pattern của log, phân log theo ngày, đặt số lượng file log tối đa trước khi xóa, phân log thành nhiều file khác nhau...

Đoạn config được sử dụng:

```
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} | %msg%n</pattern>
        </encoder>
    </appender>
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} | %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/app-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
```

Config set pattern cho log ở console và file, set địa chỉ file, phân chia file theo ngày và set số ngày tồn tại tối đa cho log.




